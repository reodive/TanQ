version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install --ignore-scripts --no-audit --no-fund
        - npx prisma generate
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*